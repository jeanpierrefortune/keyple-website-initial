[keyple-plugin-android-nfc](../../index.md) / [org.eclipse.keyple.plugin.android.nfc](../index.md) / [AndroidNfcProtocolSettings](index.md) / [getSetting](./get-setting.md)

# getSetting

`fun getSetting(seProtocol: SeProtocol): `[`String`](https://kotlinlang.org/api/latest/jvm/stdlib/kotlin/-string/index.html)

Associates a protocol and a string defining how to identify it (here a regex to be applied on
the ATR)

### Exceptions

`No` - such Element Exception if protocol not found in settings

**Return**
a settings map

